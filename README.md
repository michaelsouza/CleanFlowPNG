# CleanFlowPNG
- Requirements:
  - libpng
  - libpng++
  - libpng++-dev

- Installation:

```bash
sudo apt-get install libpng libpng++ libpng++-dev
```

- Compilation:

```bash
make all
```

- Execution:

```bash
./clean_image <input_image>
```